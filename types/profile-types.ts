export interface ProfileData {
  firstName: string;
  lastName: string;
  phoneNumber: string;
  email: string;
  userId: string;
  createdAt: string;
  lastSignInAt: string;
  emailVerified: boolean;
}
